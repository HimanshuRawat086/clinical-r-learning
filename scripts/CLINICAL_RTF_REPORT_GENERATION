#--------------------------------------------------
# Step 1: Load Libraries
#--------------------------------------------------
library(safetyGraphics)

library(reporter)

library(formatters)

#--------------------------------------------------
# Step 2: Load Example Data
#--------------------------------------------------
adsl <- ex_adsl

adae <- ex_adae

#--------------------------------------------------
# Step 3: Create Report Object
#--------------------------------------------------
rpt <- create_report(
  
  file = "clinical_report.rtf",
  
  output_type = "RTF"
)

#--------------------------------------------------
# Step 4: Add ADSL Content
#--------------------------------------------------
add_content(
  
  rpt,
  
  content = head(adsl)
)

#--------------------------------------------------
# Step 5: Add ADAE Content
#--------------------------------------------------
add_content(
  
  rpt,
  
  content = head(adae)
)

#--------------------------------------------------
# Step 6: Write Final Report
#--------------------------------------------------
write_report(rpt)
